<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home_fragment">

    <fragment
        android:id="@+id/home_fragment"
        android:name="com.example.fetch.HomeFragment"
        android:label="HomeFragment"
        tools:layout="@layout/home_fragment">

        <action
            android:id="@+id/action_home_fragment_to_hiring_list"
            app:destination="@id/hiring_list"/>
    </fragment>
    <fragment
        android:id="@+id/hiring_list"
        android:name="com.example.fetch.HiringListFragment"
        android:label="FetchRewardsListFragment"
        tools:layout="@layout/hiring_list">

    </fragment>

</navigation>